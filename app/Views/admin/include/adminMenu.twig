<!-- Sidebar -->
<nav class="col-12 col-sm-12 col-md-12 col-lg-2 d-md-block bg-dark sidebar" id="sidebar" role="navigation" aria-label="Menu d'administration">
	<div class="position-sticky">
		<h4 class="text-left text-white py-4 fs-4 small">Menu</h4>
		<ul class="nav flex-column">

			{% set current = currentRoute|default('') %}

			{% if checkPerm('view_site_statistics') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/dashboard' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/dashboard">
					<i class="fas fa-tachometer-alt me-3 fs-4"></i>
					Panel Administrateur
				</a>
			</li>
			{% endif %}

			{% if checkPerm('edit_own_profil') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/user/myprofil' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/user/myprofil">
					<i class="fas fa-user me-3 fs-4"></i>
					Mon Profil
				</a>
			</li>
			{% endif %}

			{% if checkPerm('manage_user_groups') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/users' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/users">
					<i class="fas fa-users me-3 fs-4"></i>
					Utilisateurs
				</a>
			</li>
			{% endif %}

			{% if checkPerm('manage_plugins') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/mods' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/mods">
					<i class="fas fa-puzzle-piece me-3 fs-4"></i>
					Modules
				</a>
			</li>
			{% endif %}

			{% if checkPerm('edit_site_settings') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/settings' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/settings">
					<i class="fas fa-cogs me-3 fs-4"></i>
					Configuration
				</a>
			</li>
			{% endif %}

			{% if checkPerm('manage_error_logs') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/logs' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/logs">
					<i class="fas fa-book me-3 fs-4"></i>
					Logs
				</a>
			</li>
			{% endif %}

			{% if checkPerm('view_site_statistics') %}
			<li class="nav-item mb-3">
				<a class="nav-link d-flex align-items-center p-3 rounded-3 small {% if current starts with '/admin/genimages' %}active bg-secondary text-white fw-bold{% else %}text-light hover-bg-dark{% endif %}" href="{{base_url}}/admin/genimages">
					<i class="fas fa-wand-magic-sparkles me-3 fs-4"></i>
					Génération d'images
				</a>
			</li>
			{% endif %}

			<li class="nav-item mb-3">
				<a class="nav-link text-light d-flex align-items-center p-3 rounded-3 hover-bg-dark small" href="{{base_url}}/logout">
					<i class="fas fa-sign-out-alt me-3 fs-4"></i>
					Déconnexion
				</a>
			</li>
		</ul>
	</div>
</nav>
